export interface ICourse {
  id: number;
  title: string;
  description: string;
  duration: number;
}

export interface ITopic {
  title: string,
  time: string,
  task: string
  description: string
}
